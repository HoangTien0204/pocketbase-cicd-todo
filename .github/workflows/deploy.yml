name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install -g @pocketbasecloud/cli@latest

      - name: Deploy
        working-directory: web
        env:
          PB_TOKEN: ${{ secrets.PB_TOKEN }}
        run: pb cloud frontend deploy --no-input
